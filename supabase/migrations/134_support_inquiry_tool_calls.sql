-- =====================================================
-- サポートボットが確認したデータの記録
-- =====================================================
-- ボットが実データ（出席・送迎・変更履歴など）を参照して回答できるように
-- なったため、「その回答の根拠として何を見たのか」を残す。
--
-- 【なぜ必要か】
-- 1. 職員・管理者がボットの回答を検算できる。何を見て言っているのか
--    分からないAIの指摘を、記録の修正判断に使わせるわけにはいかない。
-- 2. 個人情報を含むデータを外部APIへ渡した事実の監査証跡になる。
--
-- 中身は [{ "name": "get_child_day", "input": { ... } }, ...] の配列。
-- 取得した結果そのものは保存しない（肥大化するうえ、後から見ても
-- そのときのDBの状態は再現できないため。現在値は画面で確認すればよい）。

ALTER TABLE support_inquiry_messages
  ADD COLUMN IF NOT EXISTS tool_calls JSONB;
