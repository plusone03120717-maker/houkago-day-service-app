import type Anthropic from '@anthropic-ai/sdk'
import { createClient as createSupabaseClient } from '@supabase/supabase-js'

/**
 * サポートボットが使うモデル。
 *
 * 実データを読ませるにあたって Sonnet 5 と Haiku 4.5 を実際のデータで
 * 比較したが、出席記録の不整合の指摘・変更履歴の読み解きのどちらも
 * Haiku で十分な精度が出た（むしろ変更履歴の説明は Haiku の方が明快だった）。
 * 入力単価が2倍になるだけの差が観測できなかったので Haiku のままにしている。
 *
 * 運用してみて誤りが目立つようなら、Vercel の環境変数 SUPPORT_BOT_MODEL に
 * claude-sonnet-5 を入れればデプロイし直さずに切り替えられる。
 */
export const SUPPORT_MODEL = process.env.SUPPORT_BOT_MODEL || 'claude-haiku-4-5'

/**
 * 思考の深さ。Haiku 4.5 はこの指定に対応しておらず（エラーになる）、
 * Sonnet 5 などに切り替えたときだけ渡す。チャットなので浅めで十分。
 */
export const SUPPORT_EFFORT: 'low' | undefined = SUPPORT_MODEL.includes('haiku')
  ? undefined
  : 'low'

/**
 * 1回の応答で使えるトークン数。
 * 思考するモデルでは思考分もここに含まれるため、少ないと本文が出る前に
 * 打ち切られて空の回答が返る（Sonnet + 1500 で実際に発生した）。
 */
export const SUPPORT_MAX_TOKENS = 4000

/** 会話が長くなりすぎたときに Claude へ渡す直近のやり取り数 */
export const MAX_HISTORY_MESSAGES = 30

/** 1回の質問でデータ確認に使えるツール呼び出しの往復回数 */
export const MAX_TOOL_ITERATIONS = 4

const INSTRUCTIONS = `あなたは放課後等デイサービス管理アプリの社内サポート担当です。
このアプリを日々使っている職員（支援員・管理者）からの質問に答えます。

【あなたの役割】
・アプリの使い方・操作手順の案内
・「入力を間違えた」「表示がおかしい」といった相談の切り分け
・実際のデータを確認したうえでの原因の特定
・アプリの不具合と思われるものの情報整理

【データを確認する道具について】
出席・送迎・利用計画・変更履歴などの実データを読むツールが使えます。
・データに関する相談（「○○さんの△日の記録がおかしい」など）では、
  推測で答えず必ずツールで実際の値を確認してから答えてください。
・児童名が出てきたら、まず search_children で児童IDを特定します。
  候補が複数出たときは、勝手に決めずどの児童か職員に確認してください。
・日付は必ず YYYY-MM-DD 形式で渡します。「昨日」「先週の金曜」のような
  言い方は、後述の今日の日付から計算してください。年が言われていない
  ときは、今日の日付から最も自然な年を使います。
・ツールはすべて参照専用です。あなたがデータを書き換えることはできません。
・ツールで見えるのは、質問している職員がアプリの画面で見られる範囲と同じです。
  「データが見つかりません」は、存在しない場合と権限がない場合の両方があります。
・回答では、テーブル名や列名（daily_attendance、service_start_time など）を
  そのまま出さないでください。「提供時間」「出席状況」など画面上の言葉に直します。
・ツールで確認できなかったことを、確認したかのように言ってはいけません。

【2種類のマニュアルがあります】
・操作マニュアル … アプリの使い方。画面名・ボタン・手順のこと
・社内マニュアル … 法人の運用ルール・支援の方針など、事業所の中の決めごと
どちらに書かれていたことなのかが職員に伝わるよう、答えるときは
「操作マニュアルでは」「社内マニュアルでは」と根拠を示してください。
社内マニュアルが渡されていない場合は、社内ルールについては答えられません。

【絶対に守ること】
1. 回答の根拠は、後述する「操作マニュアル」と「社内マニュアル」に
   書かれている内容だけです。
   どちらにも載っていないことを、あたかも事実であるかのように答えてはいけません。
   分からないときは「マニュアルには記載がありません」と正直に伝えてください。
   とくに社内ルール（勤務・手当・報告義務・支援の方針など）は、
   一般論で答えると事実と食い違います。社内マニュアルに記載がなければ、
   推測せず「管理者に確認しましょう」と案内してください。
2. 画面名・メニュー名・ボタン名は、マニュアルの表記をそのまま使ってください。
   それらしい名前を創作してはいけません。
3. あなたはデータを書き換えることも、アプリを修正することもできません。
   「こちらで直しておきます」とは決して言わず、職員自身が操作する手順を案内するか、
   管理者への報告を促してください。
4. 「この値は間違っているので直してください」と言い切るのは、ツールで確認した
   事実から明らかなときだけにしてください。記録が正しいのに職員が直してしまうのが
   一番まずい結果です。確信が持てないときは、確認した事実を並べたうえで
   「どちらが正しいかご確認ください」と委ねてください。

【まず答える、それから聞く】
マニュアルに該当する説明があるときは、聞き返す前にまずその手順を案内してください。
「一覧に出てこない」「保存できない」のように、マニュアルに原因と対処が書かれている
相談は少なくありません。案内したうえで、それで解決しなければ状況を尋ねてください。

【不具合・入力ミスの相談を受けたとき】
マニュアルを見ても原因が絞れないときだけ、1〜2問ずつ質問して聞き出してください。
一度にたくさん質問すると答えにくいので、まとめて並べないこと。聞くべきことは主に次です。
・どの画面で起きたか
・どんな操作をしたか
・実際に何が起きたか
・本来どうなるはずだったか
・対象の児童名・日付（データに関する相談のとき）

【管理者への報告をすすめる場面】
次のいずれかに当てはまったら、回答の最後に
「この件は管理者に報告しておくのがよさそうです。下の「管理者に報告」ボタンを押してください。」
と案内してください。
・マニュアルを見てもアプリの動作がおかしいと判断できるとき
・2〜3往復しても解決しないとき
・データの修正に管理者権限が必要なとき
・機能の追加・変更の要望であるとき

【回答の書き方】
・敬体（です・ます）で、現場の職員に分かる平易な言葉を使う
・手順は番号付きの箇条書きにする
・300字程度を目安に簡潔に。前置きや謝罪の繰り返しは不要
・マークダウンの見出し記法（#）や太字記法（**）は使わない（そのまま文字として表示されます）`

/**
 * 回答からマークダウンの装飾記法を落とす。
 *
 * 会話画面はプレーンテキストで表示するため、** や ## がそのまま文字として
 * 出てしまう。プロンプトでも禁じているが、それでも時々混ざるので最後に必ず外す。
 */
export function stripMarkdown(text: string): string {
  return text
    .replace(/\*\*(.+?)\*\*/g, '$1')
    .replace(/(?<!\*)\*(?!\*)(.+?)(?<!\*)\*(?!\*)/g, '$1')
    .replace(/^#{1,6}\s+/gm, '')
    .replace(/^\s*[-*]\s+/gm, '・')
    .trim()
}

/**
 * Claude に渡す system ブロックを組み立てる。
 *
 * プロンプトキャッシュは「先頭からの完全一致」で効くため、
 *   1. 固定の指示文
 *   2. マニュアル全文（ここにキャッシュ区切りを置く）
 *   3. 毎回変わる文脈（氏名・日付・画面）
 * の順に並べる。3 を先に置くとキャッシュが毎回外れる。
 */
export function buildSystemBlocks(params: {
  manual: string | null
  internalManual: string | null
  userName: string
  role: string
  pagePath: string | null
}): Anthropic.TextBlockParam[] {
  const { manual, internalManual, userName, role, pagePath } = params

  const blocks: Anthropic.TextBlockParam[] = [{ type: 'text', text: INSTRUCTIONS }]

  if (manual) {
    blocks.push({
      type: 'text',
      text: `【操作マニュアル（アプリの使い方の根拠）】\n${manual}`,
      cache_control: { type: 'ephemeral' },
    })
  } else {
    // マニュアルを読めなかったときに、それらしい嘘を答えさせない
    blocks.push({
      type: 'text',
      text:
        '【重要】操作マニュアルを読み込めませんでした。' +
        '具体的な操作手順は案内せず、状況の聞き取りだけを行い、' +
        '「管理者に報告」ボタンからの報告を案内してください。',
    })
  }

  // 社内マニュアルは運用中に書き換わる。操作マニュアルの後ろに置き、
  // ここにも区切りを打つことで、社内マニュアルを直しても
  // 操作マニュアル分（4万トークン超）のキャッシュは効いたままになる。
  if (internalManual) {
    blocks.push({
      type: 'text',
      text: `【社内マニュアル（法人の運用ルール・支援方針の根拠）】\n${internalManual}`,
      cache_control: { type: 'ephemeral' },
    })
  }

  const today = new Intl.DateTimeFormat('ja-JP', {
    timeZone: 'Asia/Tokyo',
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  }).format(new Date())

  blocks.push({
    type: 'text',
    text: [
      '【いまの状況】',
      `今日の日付: ${today}`,
      `質問している職員: ${userName}（${role === 'admin' ? '管理者' : '支援員'}）`,
      pagePath ? `質問を始めた画面: ${pagePath}` : '質問を始めた画面: 不明',
    ].join('\n'),
  })

  return blocks
}

/**
 * 会話ログへの書き込み用クライアント。
 *
 * support_inquiries / support_inquiry_messages には一般ユーザー向けの
 * INSERT ポリシーを作っていない（他人の会話に発言を差し込めないようにするため）。
 * その代わり、呼び出し側で必ずログイン確認と本人確認を行うこと。
 */
export function createAdminClient() {
  return createSupabaseClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false } }
  )
}
